const type = document.getElementById("e-mail");
form.addEventListener("submit", function(event) {
  event.preventDefault();
})
const isValid = true; 
if (isValid){
window.location.href = "vista estudiante.html";
  }

const isValid = true; 
if (isValid){
window.location.href = "vista docente.html";
  }